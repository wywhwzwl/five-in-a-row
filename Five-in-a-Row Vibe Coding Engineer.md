---
name: Five-in-a-Row Vibe Coding Engineer
description: 专用于五子棋（Gomoku）全栈开发的 Vibe Coding 强制工作流。该 Skill 强制 AI 遵循“架构优先、渲染次之、逻辑严谨、AI 分层、审计收尾”的五步法，确保生成的代码低耦合、高可维护，且无隐蔽的逻辑 Bug。
version: 1.0.0
author: Vibe Coder Pro
trigger: 当用户要求开发、修改或重构五子棋（或类似棋盘游戏）时，自动加载本协议。
---

# 🚨 核心开发宪法 (Constitution)

在开始任何代码编写前，AI 必须严格遵循以下三条铁律，违反即视为开发失败：
1. **不可变数据原则 (Immutable State)：** 所有棋盘状态（Board State）更新必须使用深拷贝（如 `structuredClone` 或展开运算符），严禁直接修改原始二维数组，以保障悔棋与 AI 回溯的稳定性。
2. **像素级精确原则：** Canvas 点击坐标换算必须包含边框（Padding）偏移量的动态计算，严禁使用魔数（Magic Number）硬编码。
3. **分层解耦原则：** UI 渲染、游戏逻辑（胜负判定）、AI 算法必须拆分为独立的函数/模块，严禁在点击事件回调中直接编写复杂的业务循环。

---

# 📋 强制分步执行流程 (Workflow)

AI 必须严格按以下 5 个阶段依次推进，**每完成一个阶段，必须暂停并请求用户确认（输入“继续”或提出修改意见），不得跳步。**

## 阶段 0：技术选型与架构设计 (Analysis)
- **动作**：向用户提问确认技术栈（纯 HTML/CSS/JS 或 Vue/React）。
- **产出**：给出项目文件拆分方案（如：`index.html`, `style.css`, `gameEngine.js`, `aiPlayer.js`, `renderer.js`）。
- **指令模板**：*“请先不要写代码。请根据选型，定义核心数据结构 `GameState` 对象（包含 board, currentPlayer, history, gameOver），并阐述各模块间的通信方式。”*

## 阶段 1：静态视觉与棋盘渲染 (Render)
- **动作**：绘制 15x15 标准棋盘，包含星标（天元/小目），木质背景。
- **严格约束**：
  - 必须实现**鼠标悬停预览**（高亮指示器）。
  - 棋子须有径向渐变立体感，且落子带有 CSS 或 Canvas 缩放动画。
- **指令模板**：*“实现棋盘渲染器。特别注意：Canvas 的 `click` 事件监听中，计算落子坐标时，必须动态读取 Canvas 的 `getBoundingClientRect()` 来修正偏移，确保点击绝对精确。”*

## 阶段 2：核心游戏引擎 (Logic)
- **动作**：实现落子、回合切换、禁手（可选）及**胜负判定**。
- **关键算法指令**（必须照此执行）：
  - 胜负判定必须采用 **“十字双端探测法”**：以当前落子为中心，分别探测水平、垂直、主斜、副斜四个方向。
  - 每个方向需统计 `左/上` 连续数 + `右/下` 连续数 + 1（自身）。
  - 必须处理 **“平局（满盘无空位）”** 逻辑。
- **指令模板**：*“编写 `checkWin(row, col, player)` 函数。请使用 for 循环分别遍历 4 个方向向量。代码中禁止使用 `break` 跳出多层循环时产生逻辑歧义，推荐使用计数器累加方式。”*

## 阶段 3：AI 对手的分层实现 (AI)
- **动作**：实现人机对战。
- **禁止行为**：禁止一上来就编写复杂的 Minimax 决策树（极易因 Token 不足产生幻觉）。
- **渐进式指令**（AI 必须按此顺序生成代码）：
  1. **V1.0 随机模式**：仅从空位中随机选点（用于测试 UI 交互流畅度）。
  2. **V2.0 防御模式**：扫描玩家最后一次落子周围的空位，若有活三（连续 3 子两端开放）或冲四（连续 4 子），立即堵截。
  3. **V3.0 评分模式**：为每个空位计算“双端权重分”（连子数 + 开放程度），取最高分落子。

## 阶段 4：功能增强与状态管理 (Controls)
- **动作**：添加顶部/底部控制栏。
- **必须包含功能**：
  - `重新开始`（重置 State，不清除历史记录用于后续复盘）。
  - `悔棋`（使用 **命令模式** 或 **历史快照数组** 弹出上一步，必须同时撤回玩家和 AI 的两步）。
  - `难度切换`（绑定至阶段 3 的不同 AI 版本）。

---

# 🔬 强制审计与纠偏机制 (Audit)

在阶段 4 完成后，AI **必须**主动执行一次代码质量审查，并向用户输出审查报告，而非等待用户提问。

**审查清单（必须逐条核对）：**
1. **内存泄漏检查**：Canvas 事件监听器是否正确绑定了 `AbortController`？动画循环是否使用了 `requestAnimationFrame` 并在页面隐藏时停止？
2. **全局污染检查**：是否还存在未使用 `let/const/class` 封装的全局变量？
3. **魔法数字替换**：棋盘大小（15）、格子间距（如 40px）是否已提取为 `config.js` 中的常量？
4. **性能检查**：棋盘重绘时是全量 `clearRect` 重绘，还是仅重绘变化棋子（推荐全量重绘但配合离屏渲染优化）？

---

# 🛑 异常处理与自救协议 (Self-Healing)

当 AI 在开发过程中，连续针对同一报错修改 3 次仍未通过时，AI **必须** 执行以下操作：
1. 放弃当前代码块。
2. 向用户输出：“检测到逻辑死锁/复杂度过载，建议开启新对话窗口。”
3. 提供 **“最小可复现错误日志”** 和 **“回滚至上一步稳定版本”** 的指令，指导用户在新窗口中粘贴日志并继续。

---

# 📤 最终交付标准 (Final Output)

当所有阶段完成后，AI 必须输出：
1. **单一 HTML 文件（含内联 CSS/JS）**（便于直接运行），或按阶段 0 约定的多文件结构。
2. **交互流程图**（Mermaid 格式）：清晰描述 `用户点击 -> 坐标换算 -> 状态深拷贝 -> 落子 -> 胜负判定 -> (若无胜负) AI 计算 -> 落子 -> 判定` 的完整闭环数据流。